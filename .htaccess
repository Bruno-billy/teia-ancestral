<IfModule mod_headers.c>
    # Previne que o site seja aberto dentro de um iframe falso (Proteção contra Clickjacking)
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # Previne injeção de código e roubo de sessões (Proteção XSS)
    Header always set X-XSS-Protection "1; mode=block"
    Header always set X-Content-Type-Options "nosniff"
    
    # Força os navegadores a usarem apenas conexão segura HTTPS (HSTS)
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
</IfModule>